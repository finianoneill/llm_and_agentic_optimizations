# LLM Latency Lab - Docker Environment Configuration
# Copy this file to .env and update the values

# PostgreSQL (for Langfuse)
POSTGRES_PASSWORD=langfuse-secret-change-me

# Langfuse Authentication
NEXTAUTH_SECRET=my-nextauth-secret-change-me
SALT=my-salt-value-change-me

# Langfuse API Keys (optional - generate from Langfuse UI after first setup)
LANGFUSE_PUBLIC_KEY=
LANGFUSE_SECRET_KEY=

# Note: Claude authentication uses ~/.claude from your host machine
# Make sure you have run `claude login` before starting the stack
